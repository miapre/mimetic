{
  "name": "html-to-figma-design-system",
  "mcpName": "io.github.miapre/html-to-figma-design-system",
  "version": "1.1.0",
  "description": "Convert HTML to Figma using your design system — MCP server + local bridge for DS-aware design generation via the Plugin API",
  "type": "module",
  "scripts": {
    "bridge": "node --env-file=.env bridge.js",
    "mcp": "node mcp.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.15.0",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20.6"
  }
}
