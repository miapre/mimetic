node_modules/
package-lock.json
.DS_Store

# Secrets and local tokens
.env
.mcpregistry_*

# Runtime-generated learning artifacts (user-specific, not source)
ds-knowledge.json
internal/learning/*.json
internal/learning/*.md

# Runtime-generated build outputs
internal/builds/

# Archive — preserved locally, not published
internal/_archive/

# Backups — local safety nets, not source
internal/backups/

# Research — contains DS-specific keys, user paths, deliberation docs
internal/research/

# Diagnostics — runtime debugging output
internal/diagnostics/

# Internal marketing strategy — creator-only, not for end users
internal/marketing/

# Internal logo iterations — published logos are in assets/
internal/logo/

# DS knowledge cache (generated, large)
internal/ds-knowledge/

# Build learning workspace (runtime-generated, local only)
mimic/builds/
mimic/knowledge/
mimic/reports/
mimic/config/

# Design assets — local only
Design/

# OS artifacts
*.swp
*~
.Spotlight-V100
.Trashes
Thumbs.db
